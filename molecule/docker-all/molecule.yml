---
dependency:
  name: galaxy
  options:
    role-file: requirements.yml
driver:
  name: docker
lint:
  name: yamllint
  options:
    config-file: molecule/.yamllint
platforms:
  # https://hub.docker.com/search/?type=image
  - name: instance-docker-alpine-latest
    image: alpine:latest
    command: sh -c "while true ; do sleep 1 ; done"
  - name: instance-docker-archlinux
    image: base/archlinux:latest
  - name: instance-docker-centos-latest
    image: centos:latest
  - name: instance-docker-debian-latest
    image: debian:latest
  - name: instance-docker-fedora-latest
    image: fedora:latest
  - name: instance-docker-opensuse-leap
    image: opensuse/leap:latest
  - name: instance-docker-opensuse-tumbleweed
    image: opensuse/tumbleweed:latest
  - name: instance-docker-ubuntu-latest
    image: ubuntu:latest
provisioner:
  name: ansible
  lint:
    name: ansible-lint
  config_options:
    defaults:
      callback_plugins: /usr/lib/python2.7/site-packages/ara/plugins/callbacks
  playbook:
    converge: ${MOLECULE_PLAYBOOK:-../playbook.yml}
scenario:
  name: default-all
verifier:
  name: goss
  directory: ${MOLECULE_TESTS:-../tests/}
  lint:
    name: yamllint
